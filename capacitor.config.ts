import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jamesgallegoh.inventoryapp',
  appName: 'inventoryApp',
  webDir: 'www'
};

export default config;
